"""User-defined strategy package.

The package itself is committed so clean checkouts can import
``forven.strategies.custom`` safely, while the strategy modules inside remain
gitignored local scratch files by default.
"""

